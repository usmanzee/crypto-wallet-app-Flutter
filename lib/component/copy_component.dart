import 'package:flutter/material.dart';

class CopyComponent extends StatelessWidget {
  final String text;
  CopyComponent({this.text});
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
